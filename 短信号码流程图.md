# 短信号码购买与 GoPay 账号处理流程

> 本流程适用于 SMSBower 和 HeroSMS：服务/国家/价格查询、号码购买、请求下一条短信、结算和取消均按任务所属平台执行。收码方式按平台分开：SMSBower 保留 `getStatus` 轮询；HeroSMS 的登录码、改 PIN 码和后续码全部由 webhook 驱动，不调用 HeroSMS `getStatus`。两个平台的 API Key 分开加密保存，官方接口地址内置，页面不需要 Base URL。
>
> “计划购买数量”是最终需要达到的 `fulfilled` 成功数，不是累计购号上限。号码在进入 `fulfilled` 前遇到未注册、PIN 登录要求、0 RP、验证码超时、处理失败、过期或手动删除等终态结果时，会释放处理槽位并自动补购，因此 `purchased` 可超过计划数量；`fulfilled` 达标后任务进入 `completed`。已 `fulfilled` 号码在后续收码阶段被取消、过期或删除时，保留成功计数且不触发补购。购号或落库回执未知时会冻结自动购买，避免重复扣费。
>
> 号码不再使用历史黑名单或指纹去重：不维护 `phone_history`，也不会因历史购买记录将新号码标记为 `duplicate`。仅对当前仍有未结束激活的同一号码做并发使用保护：新分配会标记为 `phone_in_use`，自动取消并释放槽位；旧激活结束后号码可再次使用。代理池为一次性资源：可用代理耗尽且没有剩余处理中号码、成功数仍未达标时，任务标记失败并停止购号。
>
> 服务、国家、价格目录和购号均使用当前选中的平台。创建任务后平台来源固化，页面切换平台不会改变旧任务；未记录来源的升级前任务默认归属 SMSBower。HeroSMS 的 `getPrices` 只提供价格数值和库存，不返回账户币种，也不含 SMSBower 供应商 ID 或 Bronze、Silver、Gold 档位；页面不会猜测或提交这些字段，实际币种在购号响应返回后保存。
>
> API Key、PIN、代理和任务草稿在服务端加密后写入 PostgreSQL；任务、购号、号码、验证码和账号状态也都以数据库为持久化来源。HeroSMS webhook 在 HTTP 处理中先保存原始 JSON、幂等指纹、处理状态、尝试次数和错误，再返回 `200` 并异步处理。浏览器不保存这些业务数据，只在新版启动时清理旧版留下的 `localStorage` 键。服务重启后会继续数据库中的活动任务和未完成号码。

```mermaid
flowchart TD
    S([开始]) --> C0["Basic Auth 登录工作台<br/>从 PostgreSQL 加载加密的 API Key 和任务草稿<br/>选择 SMSBower 或 HeroSMS"]

    C0 --> C1["getServicesList<br/>选择服务"]
    C1 --> C2["getCountries<br/>选择国家"]
    C2 --> C3["查询所选平台报价<br/>SMSBower: getPricesV3 → getPricesV2 → getPrices，选择价格/供应商<br/>HeroSMS: getPrices，只选择价格，无供应商 ID 或价格档位"]

    C3 --> C4{"PIN 是否为 6 位数字？"}
    C4 -->|否| C4A["提示重新输入 PIN"] --> C4
    C4 -->|是| D0["初始化任务并固化平台来源<br/>记录目标成功数 quantity（1–100）<br/>购买数 purchased=0，成功数 fulfilled=0，处理中 inflight=0"]

    D0 --> D1{"fulfilled 已达计划数量？"}
    D1 -->|是| END(["任务状态=completed<br/>停止后续购买"])
    D1 -->|否| D1A{"fulfilled + inflight<br/>小于计划数量？"}
    D1A -->|否| HOLD["当前成功槽位均在处理中<br/>继续运行已购号码"]
    D1A -->|是| D1B{"代理池仍有可用地址<br/>或为直连模式？"}
    D1B -->|是| D2["getNumberV2<br/>无 v2 时回退 getNumber<br/>购买 1 个号码"]
    D1B -->|否| D1C{"仍有处理中号码？"}
    D1C -->|是| HOLD
    D1C -->|否| PX["一次性代理池已耗尽<br/>没有剩余处理中号码且成功数未达标<br/>任务标记 failed，停止购号"]

    D2 --> D3{"供应商已分配号码？"}
    D3 -->|否，明确未分配| D3A["记录临时失败<br/>不增加 purchased，退避后重试"] --> D2
    D3 -->|购号回执未知| DF["冻结自动购买<br/>保留未知购号尝试待核实<br/>避免重复扣费"]
    D3 -->|是| D4["规范化号码并保存平台 + activation_id<br/>直接准入成功处理槽位<br/>purchased +1，inflight +1<br/>不查询历史号码黑名单"]
    D4 -->|落库结果未知| DF
    D4 -->|同号码已有未结束激活| PI["标记 phone_in_use<br/>自动 setStatus=8 / cancel<br/>释放 inflight 槽位，成功数未达标时补购"] --> SLOT
    D4 -->|无未结束同号码| D7["展示号码与验证码输入框<br/>绑定 activation_id"]
    D7 -->|fulfilled 前手动删除| U0["setStatus=8 / cancel<br/>隐藏当前未成功记录<br/>不保留黑名单或永久去重指纹"] --> SLOT


    D7 --> D8["触发 GoPay 登录 OTP<br/>实际重发次数=0"] --> D8W["状态=等待登录验证码"]
    D8W --> LSRC{"任务平台？"}
    LSRC -->|SMSBower| LSPOLL["getStatus 每 2 秒轮询"]
    LSPOLL -->|WAIT_CODE / WAIT_RETRY| LSWAIT["更新下次轮询时间"] --> LSPOLL
    LSPOLL -->|STATUS_OK: code| D8A["保存并提交登录 OTP"]
    LSRC -->|HeroSMS| LHWAIT["等待 PostgreSQL webhook inbox<br/>不调用 HeroSMS getStatus"]
    LHWAIT -->|HTTPS POST 到带 token 的路由| LHSTORE["鉴权、幂等落库并返回 200<br/>回调唤醒号码 worker"]
    LHSTORE -->|有效 code| D8A
    LHSTORE -->|code 为空、过旧或阶段不符| LHIGNORE["原文保留，处理状态=ignored"] --> LHWAIT
    D8A --> D9{"登录结果？"}
    LSPOLL -->|每次发送后等待超过 60 秒<br/>仍无码| LR0{"登录 OTP 已实际重发 3 次？"}
    LHWAIT -->|每次发送后等待超过 60 秒<br/>仍无码| LR0
    LR0 -->|否| LR1["任务平台 setStatus=3 / request another<br/>重新触发 GoPay 登录 OTP<br/>实际重发次数 +1"] --> D8W
    LR0 -->|是| D14["任务平台 setStatus=8 / cancel<br/>标记：登录验证码失败"] --> SLOT["终态未成功：inflight -1<br/>purchased 保留累计值<br/>释放槽位并自动补购"] --> D1

    D9 -->|需要 PIN| D10["setStatus=8 / cancel<br/>标记：PIN 登录"] --> SLOT
    D9 -->|成功| P1["查询 GoPay 余额"]
    D9 -->|未注册| D12["setStatus=8 / cancel<br/>保留记录，状态=未注册"] --> SLOT
    D9 -->|其他失败或超时| D13["记录失败原因<br/>可恢复时重试当前号码<br/>确定终态时取消"] --> SLOT

    P1 --> P2{"余额 ≥ 1 RP？"}

    P2 -->|否，0 RP| P3["setStatus=6 / finish<br/>保留处理记录<br/>标记：0RP已被使用"] --> SLOT

    P2 -->|是| P0["查询 GoPay profile<br/>判断账号是否已有 PIN"] --> P0A{"PIN 状态？"}
    P0A -->|已有 PIN| PR0["启动 reset 流程<br/>触发 GoPay 改 PIN OTP<br/>实际重发次数=0"]
    P0A -->|无 PIN| PS0["启动 setup 流程<br/>触发 GoPay 改 PIN OTP<br/>实际重发次数=0"]
    P0A -->|未知 / 404| PS0

    PR0 --> P6["等待改 PIN 验证码"]
    PS0 --> P6
    P6 -->|收到改 PIN OTP| P7["状态=正在设置 PIN<br/>验证 OTP 并先持久化验证结果<br/>避免重试时重放已消费验证码"]
    P6 -->|每次发送后等待超过 80 秒<br/>供应商仍无码| PR1{"改 PIN OTP 已实际重发 3 次？"}
    PR1 -->|否| PR2["任务平台 setStatus=3 / request another<br/>重新触发当前 setup / reset 的 GoPay OTP<br/>实际重发次数 +1"] --> P6
    PR1 -->|是| PFAIL["任务平台 setStatus=8 / cancel<br/>标记：改 PIN 验证码失败"] --> SLOT
    P7 --> P8{"提交最终 PIN 结果？"}
    P8 -->|setup 返回 GoPay-111| PR0
    P8 -->|setting_pin：结构化 HTTP 403 / GoPay-112| P112["保留 GoPay 失败详情<br/>立即 setStatus=6 / finish<br/>不标记改 PIN 成功或 fulfilled"]
    P112 --> P112A{"结算成功？"}
    P112A -->|否，暂时失败| P112B["保持结算动作<br/>继续重试 setStatus=6"] --> P112A
    P112A -->|是| SLOT
    P8 -->|setup / reset 成功| P9["状态=改 PIN 成功<br/>标记 ever_fulfilled<br/>fulfilled +1，inflight -1"]
    P9 --> P9A{"fulfilled 已达计划数量？"}
    P9A -->|是| END
    P9A -->|否| D1
    P9 --> D11["保存账号并保留号码<br/>状态=持有中<br/>purchased 累计值不变"]
    D11 --> P10["下一轮状态=等待后续验证码"] --> QSRC{"任务平台？"}
    QSRC -->|SMSBower| Q0["getStatus 持续轮询<br/>间隔：2 秒"]
    QSRC -->|HeroSMS| QH0["等待 PostgreSQL webhook inbox<br/>不调用 HeroSMS getStatus"]

    Q0 -->|WAIT_CODE / WAIT_RETRY| Q1["等待 2 秒后再次查询<br/>收到的验证码全部写入历史，不覆盖"] --> Q0
    Q0 -->|STATUS_OK: code| Q2["按用途展示并保存验证码<br/>登录验证码：XXXX<br/>改 PIN 验证码：XXX<br/>后续验证码：1. XXXX / 2. XXXX / 3. XXX"]
    Q2 --> Q4["自动 setStatus=3 / request another<br/>2 秒后继续轮询新验证码"] --> Q0

    QH0 -->|HTTPS POST 到带 token 的路由| QH1["鉴权、幂等落库并唤醒号码 worker"]
    QH1 -->|有效 code| QH2["按接收顺序保存并展示后续验证码"]
    QH2 --> QH3["自动 setStatus=3 / request another<br/>继续等待下一次 webhook"] --> QH0
    QH1 -->|code 为空或阶段不符| QH4["保留回调原文并标记 ignored"] --> QH0

    Q0 -->|STATUS_CANCEL / NO_ACTIVATION| X0["状态=已取消或过期<br/>结束该已成功记录<br/>fulfilled 保留，不补购"]
    Q0 -->|达到号码 TTL| X1["按供应商规则结束激活<br/>状态=过期<br/>fulfilled 保留，不补购"]
    QH0 -->|达到号码 TTL| X1

    Q0 -->|手动成功按钮| U1["setStatus=6 / finish<br/>状态=成功<br/>不重复增加 fulfilled"]
    Q0 -->|手动删除按钮| U2["setStatus=8 / cancel<br/>隐藏当前已成功记录<br/>fulfilled 保留，不补购<br/>不保留黑名单或永久去重指纹"]
```

`GoPay-112` 的结算分支只适用于提交新 PIN 的 `setting_pin` 阶段，且要求 GoPay 返回可解析的结构化 HTTP 403 错误；其他阶段收到 `GoPay-112` 时仍按既有失败流程执行 `setStatus=8 / cancel`。该结算分支会保留 GoPay 失败详情，不会记为 PIN 修改成功或 `fulfilled`，结算暂时失败时只继续重试结算动作；结算完成后释放处理槽位，成功数未达标时自动补购。

## 附件项目复用点

- 已有号登录入口：`app/src/opai/core/gopay_protocol_worker.py` 中的 `_login_one_manual_existing`。
- 登录底层调用：`client.login`，成功后通过 `_save_account` 保存账号。
- 验证码轮询与激活状态封装：`app/src/opai/core/sms_helpers.py`。

## 短信供应平台兼容 API 状态映射

| API 动作 | 用途 |
| --- | --- |
| `getServicesList` | 获取服务列表 |
| `getCountries` | 获取国家列表 |
| `getPricesV3` / `getPricesV2` / `getPrices` | 获取价格和库存；HeroSMS 直接使用 `getPrices` |
| `getNumberV2` / `getNumber` | 购买号码 |
| `getStatus` | 仅 SMSBower 查询验证码和激活状态；HeroSMS 收码不调用 |
| `setStatus=3` | 请求下一条验证码 |
| `setStatus=6` | 完成/结算号码 |
| `setStatus=8` | 取消号码 |

参考：[SMSBower 官方 API 文档](https://smsbower.org/api/?page=client)。HeroSMS 使用服务内置的兼容 API 端点。

## 验证码展示格式

- 登录阶段：`登录验证码：XXXX`
- 设置或修改 PIN 阶段：`改 PIN 验证码：XXX`
- 后续短信：`后续验证码：1. XXXX / 2. XXXX / 3. XXX`

每条收到的验证码都追加到历史列表，按接收顺序保留，不覆盖之前的验证码。

SMSBower 验证码状态轮询间隔为 `2 秒`；HeroSMS 由 webhook 唤醒处理，不做验证码状态轮询。
