# SMSBower 号码购买与 GoPay 账号处理流程

> 本流程按 SMSBower 客户端 API 设计：服务/国家/价格查询、号码购买、验证码轮询、重复验证码请求、结算和取消。
>
> “购买数量”是供应商成功分配且已落库的号码总数上限。号码一经买到并落库即计数，后续无论重复、未注册、需要 PIN 登录、0 RP、过期、失败或删除都不退还名额、不补购；供应商尚未分配号码的临时错误可重试，且不计数。

```mermaid
flowchart TD
    S([开始]) --> C0["加载 API Key、购买数量、服务配置、6 位数字 PIN"]

    C0 --> C1["getServicesList<br/>选择服务"]
    C1 --> C2["getCountries<br/>选择国家"]
    C2 --> C3["getPricesV3<br/>无 v3 时回退 getPrices<br/>选择价格/供应商"]

    C3 --> C4{"PIN 是否为 6 位数字？"}
    C4 -->|否| C4A["提示重新输入 PIN"] --> C4
    C4 -->|是| D0["初始化任务<br/>历史号码指纹、激活记录、已购入库数"]

    D0 --> D1{"已购入库数 < 购买数量？"}
    D1 -->|否| END([进入号码管理])
    D1 -->|是| D2["getNumberV2<br/>无 v2 时回退 getNumber<br/>购买 1 个号码"]

    D2 --> D3{"供应商已分配号码？"}
    D3 -->|否，临时错误| D3A["记录失败原因<br/>不计数，退避后重试"] --> D2
    D3 -->|是| D4["规范化号码并保存 activation_id<br/>记录落库，已购入库数 +1<br/>状态=待校验"]

    D4 --> D5{"命中历史已购号码？"}
    D5 -->|是| D6["setStatus=8 / cancel<br/>标记重复并保留记录<br/>已计数，不补购"] --> D1
    D5 -->|否| D7["展示号码与验证码输入框<br/>绑定 activation_id"]

    D7 --> D8["复用附件已有号登录功能<br/>_login_one_manual_existing → client.login<br/>状态=等待登录验证码"]
    D8 -->|收到登录 OTP| D8A["输入并提交登录 OTP"] --> D9{"登录结果？"}
    D8 -->|等待超过 180 秒| D14["SMSBower setStatus=8 / cancel<br/>标记：等待验证码超时<br/>已计数，不补购"] --> D1

    D9 -->|需要 PIN| D10["setStatus=8 / cancel<br/>标记：PIN 登录<br/>已计数，不补购"] --> D1
    D9 -->|成功| P1["查询 GoPay 余额"]
    D9 -->|未注册| D12["setStatus=8 / cancel<br/>保留记录，状态=未注册<br/>已计数，不补购"] --> D1
    D9 -->|其他失败或超时| D13["记录失败原因<br/>按策略取消或重试当前号码<br/>已计数，不补购"] --> D1

    P1 --> P2{"余额 ≥ 1 RP？"}

    P2 -->|否，0 RP| P3["setStatus=6 / finish<br/>保留历史记录<br/>标记：0RP已被使用<br/>已计数，不补购"] --> D1

    P2 -->|是| P0["查询 GoPay profile<br/>判断账号是否已有 PIN"] --> P0A{"PIN 状态？"}
    P0A -->|已有 PIN| PR0["启动 reset 流程<br/>请求修改 PIN 验证码"]
    P0A -->|无 PIN| PS0["启动 setup 流程<br/>请求设置 PIN 验证码"]
    P0A -->|未知 / 404| PS0

    PR0 --> P6["接收改 PIN 验证码"]
    PS0 --> P6
    P6 --> P7["状态=正在设置 PIN<br/>验证 OTP 并先持久化验证结果<br/>避免重试时重放已消费验证码"]
    P7 --> P8{"提交最终 PIN 结果？"}
    P8 -->|setup 返回 GoPay-111| PR0
    P8 -->|setup / reset 成功| P9["状态=改 PIN 成功"]
    P9 --> D11["保存账号并保留号码<br/>状态=持有中<br/>购买计数不变"]
    D11 --> P10["下一轮状态=等待后续验证码"] --> Q0["getStatus 持续轮询<br/>间隔：2 秒"]

    Q0 -->|WAIT_CODE / WAIT_RETRY| Q1["等待 2 秒后再次查询<br/>收到的验证码全部写入历史，不覆盖"] --> Q0
    Q0 -->|STATUS_OK: code| Q2["按用途展示并保存验证码<br/>登录验证码：XXXX<br/>改 PIN 验证码：XXX<br/>后续验证码：1. XXXX / 2. XXXX / 3. XXX"]
    Q2 --> Q4["自动 setStatus=3 / request another<br/>2 秒后继续轮询新验证码"] --> Q0

    Q0 -->|STATUS_CANCEL / NO_ACTIVATION| X0["状态=已取消或过期<br/>保留历史记录<br/>已计数，不补购"] --> D1
    Q0 -->|达到号码 TTL| X1["按供应商规则结束激活<br/>状态=过期<br/>已计数，不补购"] --> D1

    Q0 -->|手动成功按钮| U1["setStatus=6 / finish<br/>状态=成功<br/>购买计数不变"] --> D1
    Q0 -->|手动删除按钮| U2["setStatus=8 / cancel<br/>删除展示记录，保留去重指纹<br/>已计数，不补购"] --> D1
```

## 附件项目复用点

- 已有号登录入口：`app/src/opai/core/gopay_protocol_worker.py` 中的 `_login_one_manual_existing`。
- 登录底层调用：`client.login`，成功后通过 `_save_account` 保存账号。
- 验证码轮询与激活状态封装：`app/src/opai/core/sms_helpers.py`。

## SMSBower 状态映射

| API 动作 | 用途 |
| --- | --- |
| `getServicesList` | 获取服务列表 |
| `getCountries` | 获取国家列表 |
| `getPricesV3` / `getPrices` | 获取价格和库存 |
| `getNumberV2` / `getNumber` | 购买号码 |
| `getStatus` | 查询验证码和激活状态 |
| `setStatus=3` | 请求下一条验证码 |
| `setStatus=6` | 完成/结算号码 |
| `setStatus=8` | 取消号码 |

参考：[SMSBower 官方 API 文档](https://smsbower.org/api/?page=client)

## 验证码展示格式

- 登录阶段：`登录验证码：XXXX`
- 设置或修改 PIN 阶段：`改 PIN 验证码：XXX`
- 后续短信：`后续验证码：1. XXXX / 2. XXXX / 3. XXX`

每条收到的验证码都追加到历史列表，按接收顺序保留，不覆盖之前的验证码。

验证码状态轮询间隔：`2 秒`。
